{
  "name": "dynamo-local-load",
  "version": "1.0.0",
  "description": "Short node program to sync the local DynamoDB with backups stored in an S3 bucket.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "local-load": "node local-load.js"
  },
  "keywords": [],
  "author": "Zane Littrell",
  "license": "Apache",
  "dependencies": {
    "aws-sdk": "^2.552.0"
  }
}
